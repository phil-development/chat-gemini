# chat-gemini

Modal de chat com IA (Gemini) com persistência de contexto em Postgres.

Monorepo com `frontend/` (Vite + React) e `backend/` (Fastify + TypeScript), orquestrado via Docker Compose.

Documentação completa será adicionada ao final da implementação.
